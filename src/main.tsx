import React from 'react';import ReactDOM from 'react-dom/client';import {BrowserRouter} from 'react-router-dom';import {JourneyProvider} from './state/JourneyContext';import {AppShell} from './components/AppShell';import {AppRoutes} from './routes/AppRoutes';import './styles.css';
class ErrorBoundary extends React.Component<{children:React.ReactNode},{error:boolean}>{state={error:false};static getDerivedStateFromError(){return{error:true}}render(){return this.state.error?<main><h1>We lost your place on this screen.</h1><p>Your saved prototype data remains on this device. Return home and continue when ready.</p><a href="/">Return home</a></main>:this.props.children}}
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><ErrorBoundary><BrowserRouter><JourneyProvider><AppShell><AppRoutes/></AppShell></JourneyProvider></BrowserRouter></ErrorBoundary></React.StrictMode>);
